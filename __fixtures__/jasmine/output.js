describe('', function () {});
describe('', () => {});
it('', async function () {});
it('', async () => {});
async function x() {
  expect((await global.count())).toBe(Number.EPSILON);
}
async function y() {
  expect((await global.count())).toBe(Number.EPSILON);
}
